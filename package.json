{
    "name": "Themer",
    "version": "1.0.0",
    "description": "Semantic UI Themer",
    "private": true,
    "sideEffects": false,
    "scripts": {
        "build-semantic": "cd semantic && gulp build-css build-assets",
        "watch-semantic": "cd semantic && yarn run build-semantic && gulp watch",
        "start": "yarn run build-semantic",
        "build": "yarn run build-semantic"
      },
    "devDependencies": {
        "semantic-ui": "^2.4.2"
    },
    "dependencies": {}
}
